{
  "name": "nouns-owners",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ nouns-owners",
    "create-local": "graph create --node http://localhost:8020/ nouns-owners",
    "remove-local": "graph remove --node http://localhost:8020/ nouns-owners",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nouns-owners"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
